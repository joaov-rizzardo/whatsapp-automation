import { Settings } from "lucide-react";

import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ConfiguracoesPage() {
  return (
    <PagePlaceholder
      icon={Settings}
      title="Configurações"
      description="Conexão do WhatsApp, organização e preferências vão ficar aqui. Ainda estamos construindo esta tela."
    />
  );
}
