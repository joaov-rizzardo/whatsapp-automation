import { Users } from "lucide-react";

import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function LeadsPage() {
  return (
    <PagePlaceholder
      icon={Users}
      title="Leads"
      description="Aqui vão ficar seus contatos e o funil de leads. Ainda estamos construindo esta tela."
    />
  );
}
